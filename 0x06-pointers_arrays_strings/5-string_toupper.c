#include "holberton.h"

/**
 * *string_toupper - function changes all lowercase letters to uppercase
 *
 *@n: char * pointer
 *
 * Return: char *
 */

char *string_toupper(char *)
{
	char n = *;

	while (*n >= 'a' && *n <= 'z')
		*n = ('A' - 'a');
	n++;
	return (char *);
}
